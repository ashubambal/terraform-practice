apiVersion: v1
kind: Service
metadata:
  labels:
    app: jenkins-svc
  name: jenkins-svc
  namespace: jenkins
spec:
  ports:
  - name: jenkins-svc
    port: 8080
    protocol: TCP
    targetPort: 8080
    nodePort: 30007
  selector:
    app: jenkins-dep
  type: NodePort
status:
  loadBalancer: {}
